namespace tm20trial.Domain.Constants;

public abstract class Roles
{
    public const string Administrator = nameof(Administrator);
    public const string Mapper = nameof(Mapper);
    public const string Player = nameof(Player);
}
